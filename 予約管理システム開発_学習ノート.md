# 予約管理システム開発｜学習ノート（復習用）

最終更新：2026年6月21日

---

## 1. 開発環境の全体像

```
Cursor（コードエディタ + AI）
  ├─ WSL（Windows上のLinux環境）
  ├─ Git / GitHub（コードの保存・バージョン管理）
  ├─ Node.js / npm（JavaScript実行環境・パッケージ管理）
  └─ ターミナル（Ctrl + J で開く）
```

**覚えておくこと**
- AIへの指示は右側の「チャット欄」、コマンド実行は下部の「ターミナル」。混同しやすいので要注意。
- ターミナルでコマンドを打つときは半角入力中のEnterキーに注意（誤送信の原因になりやすい）。

---

## 2. Webアプリの基本構造（3層）

| 層 | 役割 | 今回使った技術 |
|---|---|---|
| HTML | ページの骨格（構造） | `.html`ファイル |
| CSS | 見た目・デザイン | HTML内の`<style>`または別ファイル |
| JavaScript | 動き・機能（ボタンを押したら〇〇する等） | HTML内の`<script>` |

さらにWebアプリとして「保存」「複数人での共有」をするには、以下が必要になる。

```
お客さん（ブラウザ）
    ↓ データを送る
サーバー（Node.js + Express）
    ↓ データを保存する
データベース（Supabase）
```

---

## 3. 環境構築でやったこと（チェックリスト）

- [x] Cursor インストール・ログイン
- [x] WSL インストール
- [x] GitHub アカウント作成
- [x] Git for Windows インストール・初期設定
  ```bash
  git config --global user.name "名前"
  git config --global user.email "メールアドレス"
  ```
- [x] Node.js・npm インストール（`winget install OpenJS.NodeJS.LTS`）
- [x] PowerShellのスクリプト実行ポリシー変更（npmエラー対応）
  ```powershell
  Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
  ```

---

## 4. Git / GitHub の基本コマンド

| コマンド | 意味 |
|---|---|
| `git init` | このフォルダをGit管理下に置く |
| `git add .` | 変更したファイルを「保存対象」に追加 |
| `git commit -m "メッセージ"` | 変更内容を記録（セーブポイント作成） |
| `git push` | GitHubのクラウドにアップロード |
| `git --version` | Gitが入っているか確認 |

**GitHubは無料**（2019年以降、非公開リポジトリも無料枠で使える）。

---

## 5. サーバー構築（Node.js + Express）

### 基本の流れ
```bash
npm init -y          # package.jsonを作る（プロジェクトの設定ファイル）
npm install express  # Expressというサーバー用ライブラリを入れる
node server.js       # サーバーを起動する
```

### server.js が担う役割
- `GET` … データを取得する（一覧を見る）
- `POST` … データを送信する（新しい予約を登録する）
- `DELETE` … データを削除する

### サーバーの再起動手順（重要・頻出）
```
Ctrl + C          → 今動いているサーバーを止める
node server.js    → 再度起動する
```
**「Ctrl+Cで `^C` が出ただけ」では再起動完了していない。** 必ず`node server.js`をもう一度実行し、`Server running at ...`が出るまで確認する。

---

## 6. データベース（Supabase）

### 基本概念
- Supabase = 無料で使えるクラウドデータベース
- テーブル（表）にデータを保存する。Excelの表をイメージするとわかりやすい
- `.env`ファイルにURLとAPIキーを保存し、コードから読み込む

### 重要な注意点
- **APIキーは絶対にチャットや人に見せる場所に貼らない**
- 無料プランは長期間アクセスがないと自動で一時停止する（Pausedになる）→ ダッシュボードから「Resume project」で再開可能

### RLS（Row Level Security）とは
「誰が・どのデータに・何の操作（読む/書く/消す）をできるか」を制御する仕組み。
```sql
-- 例：anon（匿名ユーザー）に全操作を許可
CREATE POLICY "anon_all" ON reservations
FOR ALL TO anon USING (true) WITH CHECK (true);
```
※ `USING (true)`は「制限なし」という意味。学習段階ではOKだが、本番運用前にはより厳格な設定が必要。

### よく使うSQL（列の確認）
```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'テーブル名' 
ORDER BY ordinal_position;
```
→ エラーが「列が存在しない」系のときは、まずこれで実際の構造を確認する癖をつける。

---

## 7. デプロイ（インターネット公開）｜Railway

### 流れ
1. コードをGitHubにpush
2. RailwayでGitHubリポジトリを連携
3. Variablesタブで環境変数（`SUPABASE_URL`、`SUPABASE_ANON_KEY`）を設定
4. Settings → Networking →「Generate Domain」でURL発行

### つまずきやすいポイント
- **ポート番号**：ローカルでは3000番固定でもOKだが、本番環境では`process.env.PORT`を使う必要がある
  ```javascript
  const PORT = process.env.PORT || 3000;
  ```
- **devDependencies問題**：本番環境（`NODE_ENV=production`）では`devDependencies`がインストールされないため、`dotenvx`などのツールはクラッシュの原因になりやすい。`dependencies`に入れるか、本番では使わない設計にする。

---

## 8. 今回の最大のトラブルと解決法（要復習）

### トラブル①：複数のnodeプロセスが裏で動いていた
**症状**：コードを直しても反映されない、原因不明の500エラー
**確認方法**：
```powershell
Get-Process -Name node
```
**解決**：
```powershell
Get-Process -Name node | Stop-Process -Force
```
→ 1つだけ起動し直す。**「直らない」と思ったら、まずプロセスの重複を疑う。**

### トラブル②：テーブルの列名の不一致
**症状**：`column "time_slots.time" does not exist`のようなエラー
**原因**：AIが生成したSQLの列名と、実際にデータベースにある列名がズレていた
**解決**：`information_schema.columns`で実際の構造を確認 → `ALTER TABLE ... RENAME COLUMN`で統一

### トラブル③：Supabaseのスキーマキャッシュ
**症状**：列を追加したのにAPIエラーが消えない
**解決**：
```sql
NOTIFY pgrst, 'reload schema';
```

---

## 9. セキュリティ・個人情報まわりの学び

- 予約フォームに「他人の予約者名・電話番号」を表示しない設計が重要（個人情報保護）
- カレンダー形式＋空き状況表示（件数のみ）にすることで、UXと安全性を両立できる
- RLSの`USING (true)`は「誰でも削除できる」状態になりうるので、本番運用前に要見直し

---

## 10. 現在完成しているシステムの構成

```
お客さん用フォーム： /reservation.html
　→ カレンダーから日付選択 → 時間帯ごとの空き状況表示 → 予約

管理画面： /admin.html
　→ パスワード認証 → 全予約一覧表示・削除

データの流れ：
ブラウザ → Express（server.js）→ Supabase（time_slots / reservations テーブル）
```

公開URL：`https://my-first-page-production-d8d5.up.railway.app`

---

## 今後の課題（優先順位つき）

1. **優先度高**：RLSの「anon delete」警告への対応（削除権限の厳格化）
2. **優先度中**：管理画面から時間帯・席数を設定できるようにする（現在はSQLで手動編集）
3. **優先度低**：分単位の柔軟な時間設定、店舗ごとのマルチテナント化（複数店舗対応）
